# NMOS Client 安全审计报告（2026-03-04）

## 1. 审计目标

对当前仓库（`nmos-client-opencode`）进行漏洞排查，重点关注：

- 配置加载与输入处理
- 网络暴露面与鉴权路径
- 线程生命周期与并发访问
- 异常路径与可触发崩溃点
- 与上游 `sony/nmos-cpp` 的安全实现差异

本报告为代码审计结论，不包含渗透测试与运行时 fuzz。

---

## 2. 审计范围

- 核心实现：`src/node.cpp`
- 对外 API：`src/node.h`
- 运行示例配置：`node_config_dev.json`
- 上游参考：`sony/nmos-cpp`（Issue/PR/Commit + 关键源码）

---

## 3. 总体结论

### 3.1 风险概览

- **高风险**：3 项
- **中风险**：3 项
- **低风险**：1 项

### 3.2 结论摘要

当前代码的主要安全风险是 **DoS/崩溃类问题** 与 **未认证控制面暴露**。

- 未发现可直接落地的 RCE 证据链
- 存在可被并发与异常输入放大的崩溃路径
- 默认部署方式下，控制面可能被同网段未授权访问

---

## 4. 详细发现（按风险等级）

## 4.1 高风险

### H-01 线程生命周期未收敛，析构可触发进程终止（DoS）

- **位置**：`node.cpp:767`，`node.cpp:173`，`node.cpp:1606`
- **触发条件**：调用 `Node::start()` 后对象析构，线程仍为 joinable；或重复 `start` 导致线程状态失控
- **影响**：`std::terminate` 导致进程退出，服务中断
- **利用难度**：低（本地 API 调用即可触发）
- **类型**：DoS
- **最小修复建议**：
  - 增加 `start/stop` 状态机
  - 析构中执行可控停机并 `join`
  - 阻止重复 `start`

### H-02 回调与共享容器并发访问，存在数据竞争/崩溃风险

- **位置**：`node.cpp:252`，`node.cpp:260`，`node.cpp:279`，`node.cpp:1639`，`node.cpp:1644`
- **触发条件**：连接激活回调执行时，其他线程同时修改接收器容器或替换回调
- **影响**：未定义行为、崩溃（DoS），可能出现悬挂引用
- **利用难度**：中（需要并发时序）
- **类型**：DoS
- **最小修复建议**：
  - 为回调函数与 receiver 容器引入一致的锁策略
  - 锁内快照，锁外调用用户回调

### H-03 控制面未见显式鉴权接线，存在未授权访问面

- **位置**：`node.cpp:188`，`node.cpp:690`，`node_config_dev.json:2`，`node_config_dev.json:79`
- **触发条件**：服务在内网地址监听且未额外部署 ACL/鉴权
- **影响**：未授权客户端可访问控制面接口，可能触发配置篡改与 DoS 放大
- **利用难度**：低（同网段可达）
- **类型**：未授权访问、DoS 放大
- **最小修复建议**：
  - 默认仅绑定回环/管理网
  - 启用并校验鉴权路径
  - 部署层增加 ACL 与反向代理策略

---

## 4.2 中风险

### M-01 `transport_params` 访问缺少统一边界校验

- **位置**：`node.cpp:229`，`node.cpp:231`，`node.cpp:233`，`node.cpp:235`
- **触发条件**：输入中 `transport_params` 为空或结构异常
- **影响**：异常抛出，请求失败，可能放大为可用性问题
- **利用难度**：中
- **类型**：DoS（异常路径）
- **最小修复建议**：访问前统一检查 `is_array` 与 `size`

### M-02 `find + erase` 未校验 `end()`，存在未定义行为

- **位置**：`node.cpp:1342`，`node.cpp:1346`，`node.cpp:1350`，`node.cpp:1388`，`node.cpp:1392`，`node.cpp:1396`
- **触发条件**：索引容器与资源状态不一致（并发/异常中断后）
- **影响**：潜在崩溃（DoS）
- **利用难度**：中
- **类型**：DoS
- **最小修复建议**：`find != end` 后再 `erase`，并加入一致性校验与降级清理

### M-03 资源查找后判空不完整，可能空迭代器解引用

- **位置**：`node.cpp:476`，`node.cpp:477`，`node.cpp:478`
- **触发条件**：`flow/source` 缺失或状态竞争
- **影响**：崩溃（DoS）
- **利用难度**：中
- **类型**：DoS
- **最小修复建议**：对 `flow/source` 的查找结果逐一判空并返回受控错误

---

## 4.3 低风险

### L-01 日志可能泄露运行配置与传输参数细节

- **位置**：`node.cpp:682`，`node.cpp:529`
- **触发条件**：日志可被读取（文件、容器日志、标准输出）
- **影响**：环境与网络细节泄露
- **利用难度**：低
- **类型**：信息泄露
- **最小修复建议**：对 settings/transport 日志做脱敏与级别收敛

---

## 5. 与上游 `sony/nmos-cpp` 对照结论

### 5.1 已验证的上游安全线索

- 并发关闭崩溃问题（已修）：
  - Issue #189：<https://github.com/sony/nmos-cpp/issues/189>
  - PR #191：<https://github.com/sony/nmos-cpp/pull/191>
  - 合并提交：`0757c01454c95777ab18951bc65669c34276f7a1`

- 连接参数/约束错误导致崩溃线索：
  - Issue #182：<https://github.com/sony/nmos-cpp/issues/182>
  - 讨论显示：错误 constraints 可导致崩溃，修正后转为受控 400

### 5.2 关键差异

当前仓库在安全“接线层”仍有弱于上游常见实践的部分：

- 2026-07 更新：已接入 `on_validate_connection_resource_patch`，用于 receiver immediate activation 的 websocket 连接确认前置校验
- 未见完整鉴权验证回调链路接线
- 线程销毁/停机路径未形成上游等价的生命周期收敛

---

## 6. 修复优先级建议

### P0（立即）

1. 线程生命周期闭环：`start/stop/join` + 重复启动防护
2. 回调/容器并发保护（统一锁策略）

### P1（短期）

1. `transport_params` 全路径边界校验
2. `find+erase` 防御式检查
3. `flow/source` 判空完备化

### P2（中期）

1. 日志脱敏与审计日志规范化
2. 压测场景下的并发稳定性回归（高频 add/remove + 激活回调）

---

## 7. 风险声明与边界

- 本报告基于静态代码审计与上游公开信息对照
- 未执行黑盒渗透、网络扫描、运行时 fuzz
- 结论适用于当前审计时点代码状态
